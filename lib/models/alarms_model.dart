import 'package:miprocesoapp/utils/values/texts.dart';

class AlarmsModel{
  String procesId;
  String note;
  AlarmsModel({required this.procesId, required this.note});
}

List<AlarmsModel> alarmsModel = [
  AlarmsModel(note: note, procesId: processId),
  AlarmsModel(note: note, procesId: processId),
  AlarmsModel(note: note, procesId: processId),
  AlarmsModel(note: note, procesId: processId),
  AlarmsModel(note: note, procesId: processId),
  AlarmsModel(note: note, procesId: processId),
  AlarmsModel(note: note, procesId: processId),
  AlarmsModel(note: note, procesId: processId),
  AlarmsModel(note: note, procesId: processId),
  AlarmsModel(note: note, procesId: processId),
  AlarmsModel(note: note, procesId: processId),
];
